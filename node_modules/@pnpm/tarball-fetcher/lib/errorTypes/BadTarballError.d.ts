import { PnpmError } from '@pnpm/error';
export declare class BadTarballError extends PnpmError {
    expectedSize: number;
    receivedSize: number;
    constructor(opts: {
        attempts?: number;
        expectedSize: number;
        receivedSize: number;
        tarballUrl: string;
    });
}
