import type { DeferredManifestPromise, PackageFileInfo } from '@pnpm/cafs-types';
import ssri from 'ssri';
export interface PackageFilesIndex {
    name?: string;
    version?: string;
    files: Record<string, PackageFileInfo>;
    sideEffects?: Record<string, Record<string, PackageFileInfo>>;
}
export declare function checkPkgFilesIntegrity(cafsDir: string, pkgIndex: PackageFilesIndex, manifest?: DeferredManifestPromise): Promise<boolean>;
type FileInfo = Pick<PackageFileInfo, 'size' | 'checkedAt'> & {
    integrity: string | ssri.IntegrityLike;
};
export declare function verifyFileIntegrity(filename: string, expectedFile: FileInfo, deferredManifest?: DeferredManifestPromise): Promise<boolean>;
export {};
