/// <reference types="node" />
export declare class GcpStorage {
    private bucketName;
    private proxyConfig?;
    private storageAPI;
    private accessKey?;
    private secretKey?;
    private minioClient;
    constructor(bucketName: string, proxyConfig?: {}, storageAPI?: string, accessKey?: string, secretKey?: string);
    private getMinioClient;
    private getStorageAPIUrl;
    getFiles(opts: {
        prefix: string;
    }): Promise<any>;
    putFile(objectName: string, content: string | Buffer, metadata?: Record<string, any>): Promise<import("minio").UploadedObjectInfo>;
    static create(bucketName: string, proxyConfig?: {}, accessKey?: string, secretKey?: string): GcpStorage;
}
