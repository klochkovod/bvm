import { CopyOptions } from 'fs-extra';
declare type LoaderOpts = {
    loader: any;
};
declare type CopyToDirOpts = CopyOptions & Partial<LoaderOpts>;
export declare class FsTarVersion {
    path: string;
    constructor(path: string);
    get fileName(): string;
    get dirName(): string;
    get version(): string;
    copyToDir(destinationDir: string, opts: CopyToDirOpts): Promise<FsTarVersion>;
}
export {};
