import { ListOptions } from '@teambit/bvm.list';
import { FsTarVersion } from '@teambit/bvm.fs-tar-version';
export declare type FetchOpts = ListOptions & {
    override?: boolean;
    overrideDir?: boolean;
    destinationDir: string;
};
export declare type FetchResults = {
    resolvedVersion: string;
    downloadedFile?: string;
};
export declare function fetch(version: string, opts: FetchOpts): Promise<FsTarVersion>;
