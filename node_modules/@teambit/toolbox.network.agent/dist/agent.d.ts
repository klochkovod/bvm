import { AgentOptions as PnpmAgentOptions } from '@pnpm/network.agent';
export interface AgentOptions extends Omit<PnpmAgentOptions, 'strictSsl'> {
    cafile?: string;
    strictSSL?: boolean;
}
export declare function getAgent(uri: string, opts: AgentOptions): any;
