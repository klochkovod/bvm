import { PathExtenderReport } from '@teambit/bvm.link';
import { GcpListOptions } from '@teambit/bvm.list';
export declare type InstallOpts = GcpListOptions & {
    addToPathIfMissing?: boolean;
    override?: boolean;
    replace?: boolean;
    file?: string;
    extractMethod?: ExtractMethod;
    useSystemNode?: boolean;
};
export declare type ExtractMethod = 'default' | 'child-process';
export declare type InstallResults = {
    installedVersion: string;
    downloadRequired: boolean;
    replacedCurrent: boolean;
    previousCurrentVersion?: string;
    versionPath: string;
    pathExtenderReport?: PathExtenderReport;
    warnings?: string[];
};
export declare function installVersion(version: string, opts?: InstallOpts): Promise<InstallResults>;
