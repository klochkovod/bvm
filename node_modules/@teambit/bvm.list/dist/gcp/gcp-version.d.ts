import { RemoteVersion } from '../version';
import { ReleaseType } from './gcp-list';
export declare class GcpVersion {
    version: string;
    private fileName;
    private host;
    private md5hash;
    private timeCreated;
    private metadata;
    private protocol;
    private releaseType?;
    constructor(version: string, fileName: string, host: string, md5hash: string, timeCreated: string, metadata: {
        [key: string]: string;
    }, protocol?: string, releaseType?: ReleaseType);
    get url(): string;
    /** is version is stable version  */
    get calculatedReleaseType(): ReleaseType;
    toRemoteVersion(): RemoteVersion;
    toObject(): {
        url: string;
        version: string;
    };
}
