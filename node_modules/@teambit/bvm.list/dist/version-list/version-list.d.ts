import { Version } from '../version';
export declare class VersionList {
    entries: Version[];
    constructor(entries: Version[]);
    latest(): Version;
    stable(): Version | undefined;
    find(version: string): Version | undefined;
    toVersionsStringArray(): string[];
    toMap(): Map<string, import("..").ReleaseType>;
    sortBySemver(order?: 'asc' | 'desc'): VersionList;
    static sortList<T extends Version>(list: T[], order?: 'asc' | 'desc'): T[];
}
